#!/bin/zsh
set -euo pipefail

SCRIPT_DIR=${0:A:h}
REPO_ROOT=${SCRIPT_DIR:h}
PYTHON_BIN_DIR="/Users/neha/Library/Python/3.9/bin"
LOCAL_FFMPEG_DIR="${REPO_ROOT}/node_modules/ffmpeg-static"

export PATH="${PYTHON_BIN_DIR}:${LOCAL_FFMPEG_DIR}:${PATH}"

exec "${PYTHON_BIN_DIR}/whisper" "$@"
